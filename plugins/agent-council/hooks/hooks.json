{
  "description": "Agent Council activation: run the routing/applicability check before substantive work, as a layer above other skills.",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "if [ -n \"${PLUGIN_ROOT:-}\" ]; then agent_council_root=\"$PLUGIN_ROOT\"; elif [ -n \"${CLAUDE_PLUGIN_ROOT:-}\" ]; then agent_council_root=\"$CLAUDE_PLUGIN_ROOT\"; else exit 0; fi; bash \"$agent_council_root/hooks/agent-council-routing.sh\" SessionStart",
            "timeout": 5
          },
          {
            "type": "command",
            "command": "if [ -n \"${PLUGIN_ROOT:-}\" ]; then agent_council_root=\"$PLUGIN_ROOT\"; elif [ -n \"${CLAUDE_PLUGIN_ROOT:-}\" ]; then agent_council_root=\"$CLAUDE_PLUGIN_ROOT\"; else exit 0; fi; python3 \"$agent_council_root/scripts/Manage-AgentCouncilUpdates.py\" hook --event SessionStart",
            "timeout": 60,
            "statusMessage": "Checking for Agent Council updates"
          }
        ]
      }
    ],
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "if [ -n \"${PLUGIN_ROOT:-}\" ]; then agent_council_root=\"$PLUGIN_ROOT\"; elif [ -n \"${CLAUDE_PLUGIN_ROOT:-}\" ]; then agent_council_root=\"$CLAUDE_PLUGIN_ROOT\"; else exit 0; fi; bash \"$agent_council_root/hooks/agent-council-routing.sh\" UserPromptSubmit",
            "timeout": 5
          },
          {
            "type": "command",
            "command": "if [ -n \"${PLUGIN_ROOT:-}\" ]; then agent_council_root=\"$PLUGIN_ROOT\"; elif [ -n \"${CLAUDE_PLUGIN_ROOT:-}\" ]; then agent_council_root=\"$CLAUDE_PLUGIN_ROOT\"; else exit 0; fi; python3 \"$agent_council_root/scripts/Manage-AgentCouncilUpdates.py\" hook --event UserPromptSubmit",
            "timeout": 60
          }
        ]
      }
    ]
  }
}
