interface:
  display_name: "Agent Council"
  short_description: "Route substantive work to the right model tier"
  default_prompt: "Use $agent-council to route this work cost-effectively and apply governance only when risk warrants it."
policy:
  allow_implicit_invocation: true
