interface:
  display_name: "Agent Council"
  short_description: "Check if complex work needs model-tier governance"
  default_prompt: "Use $agent-council to check this work for a qualifying risk trigger before opening a Council case."
policy:
  allow_implicit_invocation: true
