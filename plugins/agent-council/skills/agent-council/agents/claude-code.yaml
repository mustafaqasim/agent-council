interface:
  display_name: "Agent Council"
  short_description: "Route work to the right model tier"
  default_prompt: "Use $agent-council to govern this work with appropriate model delegation and evidence gates."
policy:
  allow_implicit_invocation: true
